# Example environment variables for local database validation tests.
# Copy to .env locally if you use dotenv tooling, but do not commit real secrets.

DB_URL=jdbc:postgresql://localhost:5432/account_management_qa
DB_USERNAME=amp_qa_user
DB_PASSWORD=change_me_locally
DB_SCHEMA=public
