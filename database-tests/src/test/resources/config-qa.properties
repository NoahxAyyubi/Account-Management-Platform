# QA database config example.
# Do not commit real usernames, passwords, or production URLs.
#
# Recommended learning flow:
# 1. Copy these values into your local environment variables or a local-only file.
# 2. Replace the database name/table names after your PostgreSQL schema is created.
# 3. Keep secrets out of Git.

db.url=jdbc:postgresql://localhost:5432/amp_db
db.username=
db.password=
db.schema=public
